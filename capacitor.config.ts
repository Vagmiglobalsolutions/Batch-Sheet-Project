import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tadimety.batchsheet',
  appName: 'Tadimety Digital Batch Sheet',
  webDir: 'dist'
};

export default config;
